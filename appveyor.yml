init:
  - git config --global core.autocrlf input
build_script:
  - cmd: build.cmd -t appveyor
test: off
version: 1.{build}
branches:
  only:
    - master
artifacts:
  - path: publish.zip
    name: publish
